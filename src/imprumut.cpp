#include "imprumut.h"
#include <iostream>
#include <string.h>
using namespace std;
void imprumut::creazaImprumut(char nume[],char carte[])
{
    strcpy(studentName,nume);
    strcpy(bookName,carte);
}
